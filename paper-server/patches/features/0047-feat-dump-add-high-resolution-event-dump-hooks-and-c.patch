From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Bot <bot@example.com>
Date: Sun, 6 Sep 2026 23:14:22 +0600
Subject: [PATCH] feat(dump): add high-resolution event dump hooks and carpet
 tnt/drop rules


diff --git a/net/minecraft/server/level/ServerLevel.java b/net/minecraft/server/level/ServerLevel.java
index b03f7b22eb9ee7c0071cf2762130af9f8c41a445..a8bc86cd894f8be154aa1a3ac3e1b7610137ced7 100644
--- a/net/minecraft/server/level/ServerLevel.java
+++ b/net/minecraft/server/level/ServerLevel.java
@@ -802,6 +802,7 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
     public void tick(final BooleanSupplier haveTime) {
         io.papermc.paper.lab.cplay.CPlayManager.onLevelTickHead(this); // Paper Lab - Capture and Playback
         io.papermc.paper.lab.zone.LabTickZones.onLevelTickStart(this); // Paper Lab - Tick Zones
+        io.papermc.paper.lab.dump.ZoneDumpManager.onTickStart(this); // Paper Lab - Zone Event Dump
         ProfilerFiller profiler = Profiler.get();
         this.handlingTick = true;
         this.environmentAttributes().invalidateTickCache();
@@ -850,6 +851,7 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
         if (!this.isDebug() && runs) {
             long tick = this.getGameTime();
             profiler.push("blockTicks");
+            io.papermc.paper.lab.dump.ZoneDumpManager.setPhase("SCHEDULED_BLOCK_TICKS"); // Paper Lab - Zone Event Dump
             io.papermc.paper.lab.microtiming.LabMicroTiming.setPhase(io.papermc.paper.lab.microtiming.LabMicroTiming.Phase.TILE_TICK); // Paper Lab - /log microtiming
             try {
             this.blockTicks.tick(tick, paperConfig().environment.maxBlockTicks, this::tickBlock); // Paper - configurable max block ticks
@@ -857,6 +859,7 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
                 io.papermc.paper.lab.microtiming.LabMicroTiming.setPhase(io.papermc.paper.lab.microtiming.LabMicroTiming.Phase.NONE); // Paper Lab - /log microtiming
             }
             profiler.popPush("fluidTicks");
+            io.papermc.paper.lab.dump.ZoneDumpManager.setPhase("SCHEDULED_FLUID_TICKS"); // Paper Lab - Zone Event Dump
             this.fluidTicks.tick(tick, paperConfig().environment.maxFluidTicks, this::tickFluid); // Paper - configurable max fluid ticks
             profiler.pop();
         }
@@ -870,6 +873,7 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
         this.getChunkSource().tick(haveTime, true);
         profiler.popPush("blockEvents");
         if (runs) {
+            io.papermc.paper.lab.dump.ZoneDumpManager.setPhase("BLOCK_EVENTS"); // Paper Lab - Zone Event Dump
             io.papermc.paper.lab.microtiming.LabMicroTiming.setPhase(io.papermc.paper.lab.microtiming.LabMicroTiming.Phase.BLOCK_EVENT); // Paper Lab - /log microtiming
             try {
             this.runBlockEvents();
@@ -891,6 +895,7 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
 
         if (this.emptyTime < 300) {
             profiler.push("entities");
+            io.papermc.paper.lab.dump.ZoneDumpManager.setPhase("ENTITIES"); // Paper Lab - Zone Event Dump
             if (this.dragonFight != null && runs) {
                 profiler.push("dragonFight");
                 this.dragonFight.tick();
@@ -928,6 +933,7 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
                 );
             if (this.paperConfig().unsupportedSettings.ticking.blockEntities) { // Paper - option to disable ticking
             profiler.popPush("blockEntities");
+            io.papermc.paper.lab.dump.ZoneDumpManager.setPhase("BLOCK_ENTITIES"); // Paper Lab - Zone Event Dump
             this.tickBlockEntities();
             profiler.pop();
             } // Paper - option to disable ticking
@@ -949,6 +955,7 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
         profiler.pop();
         io.papermc.paper.lab.cplay.CPlayManager.onLevelTickReturn(this); // Paper Lab - Capture and Playback
         io.papermc.paper.lab.zone.LabTickZones.onLevelTick(this); // Paper Lab - Tick Zones
+        io.papermc.paper.lab.dump.ZoneDumpManager.onTickEnd(this); // Paper Lab - Zone Event Dump
     }
 
     @Override
@@ -1373,6 +1380,7 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
         if (!io.papermc.paper.lab.zone.LabTickZones.shouldTickFluid(this, pos, type)) {
             return;
         }
+        io.papermc.paper.lab.dump.ZoneDumpManager.onFluidTick(this, pos, type); // Paper Lab - Zone Event Dump
         BlockState blockState = this.getBlockState(pos);
         FluidState fluidState = blockState.getFluidState();
         if (fluidState.is(type)) {
@@ -1392,12 +1400,15 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
         }
         BlockState state = this.getBlockState(pos);
         if (state.is(type)) {
+            io.papermc.paper.lab.dump.ZoneDumpManager.onBlockTick(this, pos, type); // Paper Lab - Zone Event Dump
+            io.papermc.paper.lab.dump.ZoneDumpManager.pushDepth(); // Paper Lab - Zone Event Dump
             io.papermc.paper.lab.microtiming.LabMicroTiming.onTileTick(this, pos, type); // Paper Lab - /log microtiming
             io.papermc.paper.lab.microtiming.LabMicroTiming.pushDepth(); // Paper Lab - /log microtiming
             try {
             state.tick(this, pos, this.random);
             } finally {
                 io.papermc.paper.lab.microtiming.LabMicroTiming.popDepth(); // Paper Lab - /log microtiming
+                io.papermc.paper.lab.dump.ZoneDumpManager.popDepth(); // Paper Lab - Zone Event Dump
             }
         }
         // Paper start - rewrite chunk system
@@ -1431,6 +1442,9 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
         this.blockEvents.addAll(remainingEvents);
         for (final BlockEventData eventData : zoneEvents) {
             if (this.shouldTickBlocksAt(eventData.pos())) {
+                io.papermc.paper.lab.dump.ZoneDumpManager.onBlockEvent(this, eventData.pos(), eventData.block(), eventData.paramA(), eventData.paramB()); // Paper Lab - Zone Event Dump
+                io.papermc.paper.lab.dump.ZoneDumpManager.pushDepth(); // Paper Lab - Zone Event Dump
+                try {
                 if (this.doBlockEvent(eventData)) {
                     this.server
                         .getPlayerList()
@@ -1444,6 +1458,9 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
                             new ClientboundBlockEventPacket(eventData.pos(), eventData.block(), eventData.paramA(), eventData.paramB())
                         );
                 }
+                } finally {
+                    io.papermc.paper.lab.dump.ZoneDumpManager.popDepth(); // Paper Lab - Zone Event Dump
+                }
             } else {
                 this.blockEvents.add(eventData);
             }
@@ -1515,7 +1532,9 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
             if (currentlyTickingEntity.get() == null) {
                 currentlyTickingEntity.lazySet(entity);
             }
-            // Paper end - log detailed entity tick information
+            io.papermc.paper.lab.dump.ZoneDumpManager.onEntityTick(this, entity); // Paper Lab - Zone Event Dump
+        io.papermc.paper.lab.dump.ZoneDumpManager.pushDepth(); // Paper Lab - Zone Event Dump
+        try {
         entity.setOldPosAndRot();
         ProfilerFiller profiler = Profiler.get();
         entity.tickCount++;
@@ -1532,6 +1551,9 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
         for (Entity passenger : entity.getPassengers()) {
             this.tickPassenger(entity, passenger, isActive); // Paper - EAR 2
         }
+        } finally {
+            io.papermc.paper.lab.dump.ZoneDumpManager.popDepth(); // Paper Lab - Zone Event Dump
+        }
         // Paper start - log detailed entity tick information
         } finally {
             if (currentlyTickingEntity.get() == entity) {
@@ -1782,6 +1804,7 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
                 entity.setLabFrozen(true);
             }
             // Paper Lab end - freeze newly added entities if spawning in frozen zone
+            io.papermc.paper.lab.dump.ZoneDumpManager.onEntitySpawn(this, entity); // Paper Lab - Zone Event Dump
 
             return this.moonrise$getEntityLookup().addNewEntity(entity); // Paper - rewrite chunk system
         }
@@ -2175,7 +2198,9 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
             BlockEventData eventData = this.blockEvents.removeFirst();
             io.papermc.paper.lab.cplay.CPlayManager.onBlockEventProcessed(this); // Paper Lab - Capture and Playback
             if (this.shouldTickBlocksAt(eventData.pos()) && io.papermc.paper.lab.zone.LabTickZones.shouldRunBlockEvent(this, eventData.pos(), eventData)) {
+                io.papermc.paper.lab.dump.ZoneDumpManager.onBlockEvent(this, eventData.pos(), eventData.block(), eventData.paramA(), eventData.paramB()); // Paper Lab - Zone Event Dump
                 io.papermc.paper.lab.microtiming.LabMicroTiming.pushDepth(); // Paper Lab - /log microtiming
+                io.papermc.paper.lab.dump.ZoneDumpManager.pushDepth(); // Paper Lab - Zone Event Dump
                 try {
                 if (this.doBlockEvent(eventData)) {
                     io.papermc.paper.lab.cplay.CPlayManager.onBlockEventSuccess(this, eventData); // Paper Lab - Capture and Playback
@@ -2194,6 +2219,7 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
                 }
                 } finally {
                     io.papermc.paper.lab.microtiming.LabMicroTiming.popDepth(); // Paper Lab - /log microtiming
+                    io.papermc.paper.lab.dump.ZoneDumpManager.popDepth(); // Paper Lab - Zone Event Dump
                 }
             } else {
                 this.blockEventsToReschedule.add(eventData);
diff --git a/net/minecraft/world/level/Level.java b/net/minecraft/world/level/Level.java
index 278f029d11f3eec968a90b6ae47d3e146d3e494b..277e97f978b79cf4b13c235cdf121412ff1a9c59 100644
--- a/net/minecraft/world/level/Level.java
+++ b/net/minecraft/world/level/Level.java
@@ -1107,6 +1107,7 @@ public abstract class Level implements LevelAccessor, AutoCloseable, ca.spottedl
 
         BlockState newState = this.getBlockState(pos);
         io.papermc.paper.lab.microtiming.LabMicroTiming.onBlockStateChange(this, pos, oldState, newState); // Paper Lab - /log microtiming
+        io.papermc.paper.lab.dump.ZoneDumpManager.onBlockStateChange(this, pos, oldState, newState); // Paper Lab - Zone Event Dump
         /* // CraftBukkit
         if (newState == blockState) {
             if (oldState != newState) {
diff --git a/net/minecraft/world/level/ServerExplosion.java b/net/minecraft/world/level/ServerExplosion.java
index 0ea1ee76e00b654d142120484647b67c462acdf7..c41bfa749083c36205d812be81c37330048f7595 100644
--- a/net/minecraft/world/level/ServerExplosion.java
+++ b/net/minecraft/world/level/ServerExplosion.java
@@ -409,7 +409,8 @@ public class ServerExplosion implements Explosion {
 
             ray += 3;
 
-            float power = this.radius * (0.7F + this.level.getRandom().nextFloat() * 0.6F);
+            double randomRange = io.papermc.paper.lab.rules.LabRuleState.tntRandomRange;
+            float power = this.radius * (randomRange >= 0.0D ? (float) randomRange : (0.7F + this.level.getRandom().nextFloat() * 0.6F));
 
             do {
                 final int blockX = Mth.floor(currX);
@@ -552,6 +553,7 @@ public class ServerExplosion implements Explosion {
     }
 
     private void interactWithBlocks(final List<BlockPos> targetBlocks) {
+        io.papermc.paper.lab.dump.ZoneDumpManager.onExplosion(this.level, this.center.x, this.center.y, this.center.z, this.radius, targetBlocks);
         List<ServerExplosion.StackCollector> stacks = new ArrayList<>();
         Util.shuffle(targetBlocks, this.level.random);
 
diff --git a/net/minecraft/world/level/block/Block.java b/net/minecraft/world/level/block/Block.java
index 29f5182c8528bfdced6a3c2c145e58721e723777..24be13c42a8eaec0ac4163fb8815c27e6612613e 100644
--- a/net/minecraft/world/level/block/Block.java
+++ b/net/minecraft/world/level/block/Block.java
@@ -458,11 +458,17 @@ public class Block extends BlockBehaviour implements ItemLike {
 
     public static void popResource(final Level level, final BlockPos pos, final ItemStack itemStack) {
         double halfHeight = EntityTypes.ITEM.getHeight() / 2.0;
-        RandomSource random = level.getRandom();
-        double x = pos.getX() + 0.5 + Mth.nextDouble(random, -0.25, 0.25);
-        double y = pos.getY() + 0.5 + Mth.nextDouble(random, -0.25, 0.25) - halfHeight;
-        double z = pos.getZ() + 0.5 + Mth.nextDouble(random, -0.25, 0.25);
-        popResource(level, () -> new ItemEntity(level, x, y, z, itemStack), itemStack);
+        final boolean deterministic = io.papermc.paper.lab.rules.LabRuleState.hardcodeItemDrops;
+        double x = pos.getX() + 0.5 + (deterministic ? 0.0 : Mth.nextDouble(level.getRandom(), -0.25, 0.25));
+        double y = pos.getY() + 0.5 + (deterministic ? 0.0 : Mth.nextDouble(level.getRandom(), -0.25, 0.25)) - halfHeight;
+        double z = pos.getZ() + 0.5 + (deterministic ? 0.0 : Mth.nextDouble(level.getRandom(), -0.25, 0.25));
+        popResource(level, () -> {
+            ItemEntity entity = new ItemEntity(level, x, y, z, itemStack);
+            if (deterministic) {
+                entity.setDeltaMovement(0.0, 0.0, 0.0);
+            }
+            return entity;
+        }, itemStack);
     }
 
     public static void popResourceFromFace(final Level level, final BlockPos pos, final Direction face, final ItemStack itemStack) {
@@ -471,13 +477,13 @@ public class Block extends BlockBehaviour implements ItemLike {
         int stepZ = face.getStepZ();
         double halfWidth = EntityTypes.ITEM.getWidth() / 2.0;
         double halfHeight = EntityTypes.ITEM.getHeight() / 2.0;
-        RandomSource random = level.getRandom();
-        double x = pos.getX() + 0.5 + (stepX == 0 ? Mth.nextDouble(random, -0.25, 0.25) : stepX * (0.5 + halfWidth));
-        double y = pos.getY() + 0.5 + (stepY == 0 ? Mth.nextDouble(random, -0.25, 0.25) : stepY * (0.5 + halfHeight)) - halfHeight;
-        double z = pos.getZ() + 0.5 + (stepZ == 0 ? Mth.nextDouble(random, -0.25, 0.25) : stepZ * (0.5 + halfWidth));
-        double deltaX = stepX == 0 ? Mth.nextDouble(random, -0.1, 0.1) : stepX * 0.1;
-        double deltaY = stepY == 0 ? Mth.nextDouble(random, 0.0, 0.1) : stepY * 0.1 + 0.1;
-        double deltaZ = stepZ == 0 ? Mth.nextDouble(random, -0.1, 0.1) : stepZ * 0.1;
+        final boolean deterministic = io.papermc.paper.lab.rules.LabRuleState.hardcodeItemDrops;
+        double x = pos.getX() + 0.5 + (stepX == 0 ? (deterministic ? 0.0 : Mth.nextDouble(level.getRandom(), -0.25, 0.25)) : stepX * (0.5 + halfWidth));
+        double y = pos.getY() + 0.5 + (stepY == 0 ? (deterministic ? 0.0 : Mth.nextDouble(level.getRandom(), -0.25, 0.25)) : stepY * (0.5 + halfHeight)) - halfHeight;
+        double z = pos.getZ() + 0.5 + (stepZ == 0 ? (deterministic ? 0.0 : Mth.nextDouble(level.getRandom(), -0.25, 0.25)) : stepZ * (0.5 + halfWidth));
+        double deltaX = deterministic ? 0.0 : (stepX == 0 ? Mth.nextDouble(level.getRandom(), -0.1, 0.1) : stepX * 0.1);
+        double deltaY = deterministic ? 0.0 : (stepY == 0 ? Mth.nextDouble(level.getRandom(), 0.0, 0.1) : stepY * 0.1 + 0.1);
+        double deltaZ = deterministic ? 0.0 : (stepZ == 0 ? Mth.nextDouble(level.getRandom(), -0.1, 0.1) : stepZ * 0.1);
         popResource(level, () -> new ItemEntity(level, x, y, z, itemStack, deltaX, deltaY, deltaZ), itemStack);
     }
 
diff --git a/net/minecraft/world/level/block/piston/PistonMovingBlockEntity.java b/net/minecraft/world/level/block/piston/PistonMovingBlockEntity.java
index 3fcb1a45a547681740d6ec8946b274539448cf95..7716901bd730191b73fe70b965e9d72d8a21bf25 100644
--- a/net/minecraft/world/level/block/piston/PistonMovingBlockEntity.java
+++ b/net/minecraft/world/level/block/piston/PistonMovingBlockEntity.java
@@ -313,6 +313,7 @@ public class PistonMovingBlockEntity extends BlockEntity {
 
     public static void tick(final Level level, final BlockPos pos, final BlockState state, final PistonMovingBlockEntity entity) {
         entity.lastTicked = level.getGameTime();
+        io.papermc.paper.lab.dump.ZoneDumpManager.onPistonMovingTick(level, pos, entity.progress, entity.extending, entity.movedState); // Paper Lab - Zone Event Dump
         entity.progressO = entity.progress;
         if (entity.progressO >= 1.0F) {
             if (level.isClientSide() && entity.deathTicks < 5) {
